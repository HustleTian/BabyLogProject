/** * Sample React Native App * https://github.com/facebook/react-native * @flow */import React, { Component,AsyncStorage } from 'react';import {Navigator} from 'react-native-deprecated-custom-components';import WelcomePage from './view/WelcomePage';export default class App extends Component<{}> {	/**	 * 配置场景动画	 * @param route 路由	 * @param routeStack 路由栈	 * @returns {*} 动画	 */	configureScene(route, routeStack) {		if (route.type == 'Bottom') {			return Navigator.SceneConfigs.FloatFromBottom; // 底部弹出		}		return Navigator.SceneConfigs.PushFromRight; // 右侧弹出	}		/**	 * 使用动态页面加载	 * @param route 路由	 * @param navigator 导航器	 * @returns {XML} 页面	 */	renderScene(route, navigator) {		return <route.component navigator={navigator}  {...route.passProps} />;	}		render() {		var pageName = 'WelcomePage';		var component = WelcomePage;				try {// try catch 捕获异步执行的异常			var value = await AsyncStorage.getItem(STORAGE_KEY);			if (value !== null){				console.log('_get() success: ' ,value);			} else {				console.log('_get() no data');			}		} catch (error) {			console.log('_get() error: ',error.message);		}				return (            <Navigator                //指定了默认的页面，也就是启动app之后会看到的第一屏，需要两个参数，name跟component                initialRoute={{name: pageName,component: component}}                configureScene={this.configureScene}                renderScene={this.renderScene}/>		);	}}